{
  "name": "shroom-design-bridge-plugin",
  "version": "1.0.0",
  "private": true,
  "description": "MasterGo desktop plugin for the Shroom design bridge",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "node scripts/build.js",
    "pretest": "node scripts/build.js",
    "test": "node test/mapping.test.js && node test/derive.test.js && node test/runtime-smoke.test.js",
    "verify": "npm test && npm run build"
  },
  "devDependencies": {
    "@mastergo/plugin-typings": "^2.19.1"
  }
}
